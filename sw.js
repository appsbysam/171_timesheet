self.addEventListener('push',function(event){var data={};try{data=event.data?event.data.json():{}}catch(e){};event.waitUntil(self.registration.showNotification(data.title||'Staff Timesheet',{body:data.body||'Timesheet reminder',data:{url:data.url||'./'},tag:'timesheet-reminder'}));});self.addEventListener('notificationclick',function(event){event.notification.close();event.waitUntil(clients.openWindow(event.notification.data.url||'./'));});